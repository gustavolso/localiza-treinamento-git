trigger TriigerDoVitor on Opportunity (before insert, before update) {
   /* List<Opportunity> ListaRetorno = new list<Opportunity>();
   for(Opportunity umaoportunidadeso : trigger.new ){
       
       Opportunity o = new Opportunity();
       ListaRetorno.add(o)
       
		   if(umaoportunidadeso.Stagename=='Closed Won') {
				Task NovoTeste = New Task ();
			    NovoTeste.Subject = 'Follow Up Test Task';
			    NovoTeste.WhatId = NovoTeste.Id;
               
              
			}
	}
    
    insert lista;
*/
}